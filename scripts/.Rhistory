}, error = function(e) NULL)
})
summary(interaction_models[[1]])
summary(interaction_models[[2]])
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = 3,
b = 3,
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
summary(interaction_models[[2]])
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = 4,
b = 4,
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
summary(interaction_models[[2]])
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = 5,
b = 5,
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
summary(interaction_models[[2]])
summary(interaction_models[[1]])
summary(interaction_models[[2]])
summary(interaction_models[[1]])
summary(interaction_models[[2]])
summary(interaction_models[[1]])
summary(full_model)
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = 0.8,
b = 0.8,
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = 0.9,
b = 0.9,
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
summary(interaction_models[[2]])
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = 0.5,
b = 0.5,
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
summary(interaction_models[[2]])
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = 6,
b = 6,
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
summary(interaction_models[[2]])
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = 9,
b = 9,
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
summary(interaction_models[[2]])
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = 5,
b = 5,
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
summary(interaction_models[[2]])
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = c(5,9),
b = 5,
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = c(5,9),
b = c(5,9),
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
summary(interaction_models[[2]])
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = c(5,3),
b = c(5,3),
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
summary(interaction_models[[2]])
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = c(2,5),
b = c(2,5),
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
summary(interaction_models[[2]])
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = c(5,5),
b = c(5,5),
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
summary(interaction_models[[2]])
summary(full_model)
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = c(1,5),
b = c(1,5),
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
summary(interaction_models[[2]])
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = c(1,3),
b = c(1,3),
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
summary(interaction_models[[2]])
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = c(2,3),
b = c(2,3),
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = c(4,3),
b = c(4,3),
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = c(6,6),
b = c(4,3),
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
summary(interaction_models[[2]])
summary(interaction_models[[1]])
summary(interaction_models[[2]])
summary(interaction_models[[1]])
# interaction
#-5.6
interaction_models <- lapply(c(-7.1,-5.1), function(q) {
if(q == -7.1) {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff <= q)
} else {
d <- data_rdd |> dplyr::filter(gov_rrp_position_diff >= q)
}
tryCatch({
estimate_rdd(
d,
outcome = "quantmig_immigration_restrict_resid",
treatment = "right_wing_vote_share_centered",
controls = c("net_migration","net_migration_lag1"),
kernel = "epanechnikov",
#bwselect = "mserd",
h = c(5,5),
b = c(5,5),
cluster = "country_id",
FEs = c("country_id","year")
)
}, error = function(e) NULL)
})
summary(interaction_models[[1]])
summary(interaction_models[[2]])
1:10 / sum(1:10)
sum(1:10 / sum(1:10))
